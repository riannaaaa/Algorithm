public class InterviewPractice {
    /* 有一个 to do-list, 里面有多个 task, 每个 task 有一个优先级顺序。
    可以进行的操作是点击任意一个 task, 效果是它会被排列到 list 的顶端。
    问一共要进行多少次点击操作，才能将这个 t odo-list 里的 task 按照优先级整理成有序。
    这里指的并不是大 O 表示的数量级复杂度，而是给定一个特殊的 To do-list, 问需要操作的次数。
    */
//  初始思路：相当于一个int数组，假设小数字代表优先级高，大数字代表优先级低。
//  所以小数字应该都是最后被弹到top，不然又会被挤到后面
//  求次数：最坏情况：每个元素to top一次；
//        最好情况：0次；


}
